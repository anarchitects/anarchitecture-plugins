{
  "$schema": "http://json-schema.org/schema",
  "title": "Nest Init",
  "description": "Initialize the public Nest plugin generator configuration for an Nx workspace.",
  "type": "object",
  "properties": {
    "packageManager": {
      "type": "string",
      "enum": ["yarn", "npm", "pnpm"],
      "default": "yarn",
      "description": "Select the preferred package manager for future Nest workspace configuration and install task integration."
    },
    "skipPackageJson": {
      "type": "boolean",
      "default": false,
      "description": "Skip package.json dependency updates when that behavior is implemented."
    },
    "skipFormat": {
      "type": "boolean",
      "default": false,
      "description": "Skip formatting files after generation."
    },
    "forceVersions": {
      "type": "boolean",
      "default": false,
      "description": "Overwrite existing Nest dependency versions with plugin-aligned versions when dependency updates are implemented."
    }
  },
  "additionalProperties": false
}
