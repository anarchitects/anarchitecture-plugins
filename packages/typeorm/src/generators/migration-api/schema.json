{
  "$schema": "http://json-schema.org/schema",
  "title": "TypeORM Migration API Generator",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the existing Nx library project."
    },
    "name": {
      "type": "string",
      "description": "Migration name. Required when fileMode is \"new\"."
    },
    "fileMode": {
      "type": "string",
      "enum": ["new", "patch-init"],
      "default": "new",
      "description": "Create a new migration file or patch the bootstrap init migration."
    },
    "entityGlob": {
      "type": "string",
      "description": "Glob (relative to project root) used to discover entity files.",
      "default": "src/infrastructure-persistence/entities/**/*.ts"
    },
    "allowMixedMigrations": {
      "type": "boolean",
      "description": "Allow generation when manual and tool-generated migrations coexist.",
      "default": false
    },
    "timestamp": {
      "type": "number",
      "description": "Optional migration timestamp override."
    }
  },
  "required": ["project"],
  "additionalProperties": false
}
