{
  "$schema": "http://json-schema.org/schema",
  "title": "TypeORM Run Executor",
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string",
      "description": "Project root when it cannot be inferred from the Nx project configuration."
    },
    "dataSource": {
      "type": "string",
      "description": "Path to the TypeORM data source file used for migrations."
    },
    "transaction": {
      "type": "string",
      "enum": ["all", "each", "none"],
      "description": "Transaction mode passed to the TypeORM CLI."
    },
    "fake": {
      "type": "boolean",
      "description": "Mark migrations as executed without running them."
    },
    "args": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Additional CLI arguments passed through to the TypeORM command."
    }
  }
}
