{
  "$schema": "http://json-schema.org/schema",
  "title": "TypeORM Seed Executor",
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string",
      "description": "Project root when it cannot be inferred from the Nx project configuration."
    },
    "file": {
      "type": "string",
      "description": "Path to the seed script to execute."
    },
    "export": {
      "type": "string",
      "description": "Named export that should be executed. Defaults to default export."
    },
    "tsconfig": {
      "type": "string",
      "description": "Path to the tsconfig used when registering ts-node for TypeScript seeds."
    },
    "args": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Arguments passed to the seed function."
    }
  },
  "required": ["file"]
}
