{
  "$schema": "http://json-schema.org/schema",
  "title": "Nx Governance Repo Ownership",
  "type": "object",
  "properties": {
    "profile": {
      "type": "string",
      "description": "Governance profile to execute.",
      "default": "frontend-layered"
    },
    "output": {
      "type": "string",
      "enum": ["cli", "json"],
      "default": "cli"
    },
    "failOnViolation": {
      "type": "boolean",
      "default": false,
      "description": "Exit with failure if violations are found."
    },
    "conformanceJson": {
      "type": "string",
      "description": "Optional override for the Nx Conformance JSON path. Falls back to nx.json conformance.outputPath when omitted."
    }
  },
  "additionalProperties": false
}
