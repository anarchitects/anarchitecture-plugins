{
  "$schema": "http://json-schema.org/schema",
  "title": "Nx Governance AI Drift Interpretation",
  "type": "object",
  "properties": {
    "profile": {
      "type": "string",
      "description": "Governance profile used to label drift interpretation payloads.",
      "default": "angular-cleanup"
    },
    "output": {
      "type": "string",
      "enum": ["cli", "json"],
      "default": "json"
    },
    "failOnViolation": {
      "type": "boolean",
      "default": false,
      "description": "Exit with failure if deterministic assessment indicates violations."
    },
    "snapshotDir": {
      "type": "string",
      "description": "Directory where snapshots are stored.",
      "default": ".governance-metrics/snapshots"
    },
    "baseline": {
      "type": "string",
      "description": "Optional baseline snapshot path. Defaults to previous snapshot."
    },
    "current": {
      "type": "string",
      "description": "Optional current snapshot path. Defaults to latest snapshot."
    }
  },
  "additionalProperties": false
}
