{
  "$schema": "http://json-schema.org/schema",
  "title": "Nx Governance AI Refactoring Suggestions",
  "type": "object",
  "properties": {
    "profile": {
      "type": "string",
      "description": "Governance profile used to build deterministic workspace context.",
      "default": "frontend-layered"
    },
    "output": {
      "type": "string",
      "enum": ["cli", "json"],
      "default": "json"
    },
    "failOnViolation": {
      "type": "boolean",
      "default": false,
      "description": "Exit with failure if violations are found in the deterministic assessment."
    },
    "snapshotDir": {
      "type": "string",
      "description": "Directory where snapshots are stored.",
      "default": ".governance-metrics/snapshots"
    },
    "topViolations": {
      "type": "number",
      "description": "Maximum number of prioritized violations included in refactoring analysis.",
      "default": 10,
      "minimum": 1
    },
    "topProjects": {
      "type": "number",
      "description": "Maximum number of hotspot and fanout projects included in recommendations.",
      "default": 5,
      "minimum": 1
    }
  },
  "additionalProperties": false
}
