{
  "$schema": "http://json-schema.org/schema",
  "title": "Nx Governance Graph",
  "type": "object",
  "properties": {
    "profile": {
      "type": "string",
      "description": "Governance profile to execute.",
      "default": "frontend-layered"
    },
    "failOnViolation": {
      "type": "boolean",
      "default": false,
      "description": "Exit with failure if violations are found."
    },
    "conformanceJson": {
      "type": "string",
      "description": "Optional override for the Nx Conformance JSON path. Falls back to nx.json conformance.outputPath when omitted."
    },
    "format": {
      "type": "string",
      "enum": ["json", "html"],
      "default": "html",
      "description": "Output format for the governance graph artifact."
    },
    "outputPath": {
      "type": "string",
      "description": "Optional output file path. Defaults to dist/governance/graph.json or dist/governance/graph.html based on format."
    }
  },
  "additionalProperties": false
}
